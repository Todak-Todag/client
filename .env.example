# 이 파일을 복사해서 .env.local 을 만들면 개인 설정으로 덮어쓸 수 있습니다.
# .env.local 은 git에 올라가지 않습니다.
#
#   cp .env.example .env.local

# API 서버 주소 (끝에 / 를 붙이지 마세요)
VITE_API_BASE_URL=http://localhost:8080

# API 요청 타임아웃 (ms)
VITE_API_TIMEOUT=10000
